1.把失效实现写完，必须要做到通用(必须要写完的)
2.自己实现redis作为mybatis的二级缓存源码!!!(允许写不完)
    (百度:redis作为mybatis二级缓存实现--->根据百度自己去实现一下源码过程)
允许写不完，不是可以不写!!!!--->如果没有写，就直接抄项目(包括注释，注解...)

2020-03-25上课
2020-03-24需要在108上课

2020-03-25homework:
    1.把我的所有业务和代码全部弄明白
    2.现在有问题--->如果redis中有数据，并且redis中的数据和mysql中的数据不一致
            redis中是脏数据
      解决方案:
          在mysql做增删改的时候，把redis清空掉
          redis.remove();
          mybatis的缓存:(mybatis自带的缓存不方便，所以项目中根本就没有人用，公司用的就是我现在给你们写的代码)
            2.1.mybatis的第一次查询的时候会执行SQL语句查询数据库(mybatis就上缓存了)
            2.2.以后都会直接走缓存不再走mysql了
            2.3.但是当mybatis执行增删改的时候，mybatis让自己的缓存直接失效
    3.不需要页面，实现增删改查(book表的)
        要求必须要写接口文档(自己去搜ApiPost(有免费版))
        如果不知道什么是接口的话，就自行百度接口，接口不是咱们java中的interface
